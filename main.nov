// ---------------------------------------------------------------------------
// window - native window helpers
// macOS uses the existing WebKit helper; Linux/amd64 talks to X11 in-process.
// ---------------------------------------------------------------------------

module window_main;

#if(os == "darwin") {
    import darwin_arm64;
}
#if(os == "linux") {
    import linux_amd64;
}
